node_modules/
.next/
out/
*.tsbuildinfo

# Rollback aid only — regenerated by every successful refresh, and committing it
# would double the repo's daily data churn for no benefit.
data/snapshot.previous.json

.env*
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/
