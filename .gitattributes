# Normalise to LF in the repository. Developed on Windows, run on ubuntu-latest
# in CI, where a CRLF line ending inside a shell step is a syntax error.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
